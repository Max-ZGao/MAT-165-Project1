from CrossingFreePartition import CrossingFreePartition
import numpy as np

# This generates all the possible k-crossing free partitions
class PartitionGenerator:

    
    def __init__(self, objects, k):

        return


    # get the next partition
    # calling next will return a unique k-partition each time
    # if all the partitions have been returned, then return None
    # returns a array of np.array of points
    def next(self):
        
        return None

